<!DOCTYPE html>
<html>
<head>
  <title>Users</title>
</head>
<body>
  <h2>Users Page</h2>
  <ul>
    <% users.forEach(u => { %>
      <li><%= u.name %> (<%= u.role %>)</li>
    <% }) %>
  </ul>

  <a href="/">Back</a>
</body>
</html>
